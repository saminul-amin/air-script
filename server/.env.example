# Port the Express gateway listens on.
PORT=5000

# Base URL of the FastAPI AI service this gateway proxies to.
AI_SERVICE_URL=http://localhost:8000

# How long to wait for the AI service before answering 502 (ms). A sleeping
# Hugging Face Space can take about a minute to wake, so keep this generous.
UPSTREAM_TIMEOUT_MS=120000
